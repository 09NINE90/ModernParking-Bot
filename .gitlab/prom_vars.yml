deploy_prom:
  variables:
    DB_USER_VAR: $DB_USER
    DB_PASSWORD_VAR: $DB_PASSWORD
    DB_HOST_VAR: $DB_HOST
    DB_PORT_VAR: $DB_PORT
    DB_NAME_VAR: $DB_NAME
    ENV_NAME: "PROM"
    ENV_SUFFIX: "prom"
    BOT_TOKEN_VAR: $BOT_TOKEN
    GROUP_ID_VAR: $GROUP_ID
    DELAY_MINUTES_CONFIRM_SPOT_VAR: $DELAY_MINUTES_CONFIRM_SPOT
    DB_SCHEMA_VAR: $DB_SCHEMA
    TECH_GROUP_ID_VAR: $TECH_GROUP_ID
    WARN_TOPIC_ID_VAR: $WARN_TOPIC_ID
    ERROR_TOPIC_ID_VAR: $ERROR_TOPIC_ID
    INFO_TOPIC_ID_VAR: $INFO_TOPIC_ID
    DEBUG_TOPIC_ID_VAR: $DEBUG_TOPIC_ID
    STATS_TOPIC_ID_VAR: $STATS_TOPIC_ID
    FEEDBACK_TOPIC_ID_VAR: $FEEDBACK_TOPIC_ID
    CONTAINER_NAME_VAR: $CONTAINER_NAME
    WORK_DIR_VAR: $WORK_DIR